# AWS RDS - same as other gulimall services (ecommerce_sms)
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://***REMOVED***:5432/ecommerce_sms
    username: postgres
    password: ***REMOVED***
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
